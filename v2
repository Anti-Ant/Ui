getgenv().SaberFarmingStates = {
    BringBasicFire = false,
    BringAdvancedFire = false,
    BringMasterFire = false,
    BringGrandmasterFire = false,
    BringBasicWater = false,
    BringAdvancedWater = false,
    BringMasterWater = false,
    BringGrandmasterWater = false,
    BringBasicEarth = false,
    BringAdvancedEarth = false,
    BringMasterEarth = false,
    BringGrandmasterEarth = false,
    BringBasicPlasma = false,
    BringAdvancedPlasma = false,
    BringMasterPlasma = false,
    BringGrandmasterPlasma = false,
    V2 = {
        Fire = { Basic = false, Advanced = false, Master = false, Grandmaster = false },
        Water = { Basic = false, Advanced = false, Master = false, Grandmaster = false },
        Earth = { Basic = false, Advanced = false, Master = false, Grandmaster = false },
        Plasma = { Basic = false, Advanced = false, Master = false, Grandmaster = false },
        FarmActive = false
    }
}

local LocalPlayer = game:GetService("Players").LocalPlayer

-- =============================================================================
-- 2. BOOT WALLY V3 UI LIBRARY & CREATE MAIN WINDOW
-- =============================================================================
local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/Anti-Ant/Ui/refs/heads/main/Wally%20Ui')))()
local Window = library:CreateWindow("Saber Sim Hub")

-- =============================================================================
-- 3. ELEMENTS FOLDERS (Bring Toggles)
-- =============================================================================

-- Fire
local FireFolder = Window:CreateFolder("Fire Elements")
FireFolder:Toggle("Basic Fire Golem/Boss", function(bool) getgenv().SaberFarmingStates.BringBasicFire = bool end)
FireFolder:Toggle("Advanced Fire Golem/Boss", function(bool) getgenv().SaberFarmingStates.BringAdvancedFire = bool end)
FireFolder:Toggle("Master Fire Golem/Boss", function(bool) getgenv().SaberFarmingStates.BringMasterFire = bool end)
FireFolder:Toggle("Grandmaster Fire Golem/Boss", function(bool) getgenv().SaberFarmingStates.BringGrandmasterFire = bool end)

-- Water
local WaterFolder = Window:CreateFolder("Water Elements")
WaterFolder:Toggle("Basic Water Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringBasicWater = bool end)
WaterFolder:Toggle("Advanced Water Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringAdvancedWater = bool end)
WaterFolder:Toggle("Master Water Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringMasterWater = bool end)
WaterFolder:Toggle("Grandmaster Water Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringGrandmasterWater = bool end)

-- Earth
local EarthFolder = Window:CreateFolder("Earth Elements")
EarthFolder:Toggle("Basic Earth Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringBasicEarth = bool end)
EarthFolder:Toggle("Advanced Earth Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringAdvancedEarth = bool end)
EarthFolder:Toggle("Master Earth Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringMasterEarth = bool end)
EarthFolder:Toggle("Grandmaster Earth Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringGrandmasterEarth = bool end)

-- Plasma
local PlasmaFolder = Window:CreateFolder("Plasma Elements")
PlasmaFolder:Toggle("Basic Plasma Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringBasicPlasma = bool end)
PlasmaFolder:Toggle("Advanced Plasma Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringAdvancedPlasma = bool end)
PlasmaFolder:Toggle("Master Plasma Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringMasterPlasma = bool end)
PlasmaFolder:Toggle("Grandmaster Plasma Mobs/Boss", function(bool) getgenv().SaberFarmingStates.BringGrandmasterPlasma = bool end)

-- =============================================================================
-- 4. V2 UI: Separate selection toggles + master farm toggle for each element
-- =============================================================================

-- Fire V2
local FireV2 = Window:CreateFolder("Fire v2")
FireV2:Toggle("Basic Fire (v2)", function(bool) getgenv().SaberFarmingStates.V2.Fire.Basic = bool end)
FireV2:Toggle("Advanced Fire (v2)", function(bool) getgenv().SaberFarmingStates.V2.Fire.Advanced = bool end)
FireV2:Toggle("Master Fire (v2)", function(bool) getgenv().SaberFarmingStates.V2.Fire.Master = bool end)
FireV2:Toggle("Grandmaster Fire (v2)", function(bool) getgenv().SaberFarmingStates.V2.Fire.Grandmaster = bool end)

-- Water V2
local WaterV2 = Window:CreateFolder("Water v2")
WaterV2:Toggle("Basic Water (v2)", function(bool) getgenv().SaberFarmingStates.V2.Water.Basic = bool end)
WaterV2:Toggle("Advanced Water (v2)", function(bool) getgenv().SaberFarmingStates.V2.Water.Advanced = bool end)
WaterV2:Toggle("Master Water (v2)", function(bool) getgenv().SaberFarmingStates.V2.Water.Master = bool end)
WaterV2:Toggle("Grandmaster Water (v2)", function(bool) getgenv().SaberFarmingStates.V2.Water.Grandmaster = bool end)

-- Earth V2
local EarthV2 = Window:CreateFolder("Earth v2")
EarthV2:Toggle("Basic Earth (v2)", function(bool) getgenv().SaberFarmingStates.V2.Earth.Basic = bool end)
EarthV2:Toggle("Advanced Earth (v2)", function(bool) getgenv().SaberFarmingStates.V2.Earth.Advanced = bool end)
EarthV2:Toggle("Master Earth (v2)", function(bool) getgenv().SaberFarmingStates.V2.Earth.Master = bool end)
EarthV2:Toggle("Grandmaster Earth (v2)", function(bool) getgenv().SaberFarmingStates.V2.Earth.Grandmaster = bool end)

-- Plasma V2
local PlasmaV2 = Window:CreateFolder("Plasma v2")
PlasmaV2:Toggle("Basic Plasma (v2)", function(bool) getgenv().SaberFarmingStates.V2.Plasma.Basic = bool end)
PlasmaV2:Toggle("Advanced Plasma (v2)", function(bool) getgenv().SaberFarmingStates.V2.Plasma.Advanced = bool end)
PlasmaV2:Toggle("Master Plasma (v2)", function(bool) getgenv().SaberFarmingStates.V2.Plasma.Master = bool end)
PlasmaV2:Toggle("Grandmaster Plasma (v2)", function(bool) getgenv().SaberFarmingStates.V2.Plasma.Grandmaster = bool end)




local TestFolder = Window:CreateFolder("Test")
TestFolder:Button("Teleport to Spawn", function()
    local cf = CFrame.new(500, 190, 150)  -- known safe spawn
    pcall(function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = cf
            print("✅ Teleported to spawn!")
        else
            warn("❌ Character or HumanoidRootPart missing")
        end
    end)
end)





-- Master Toggle (starts the entire V2 farm loop)
local MasterV2 = Window:CreateFolder("V2 Control")
MasterV2:Toggle("Start Farm (v2)", function(bool)
    getgenv().SaberFarmingStates.V2.FarmActive = bool
end)

-- =============================================================================
-- 5. CORE BACKGROUND ENGINE (Bring toggles – pulls mobs to player)
-- =============================================================================
local function pullEnemiesToPlayer(area, myRoot)
    if not area or not myRoot then return end
    for _, mob in ipairs(area:GetChildren()) do
        if mob:IsA("Model") then
            local hrp = mob:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.Anchored = true
                hrp.CFrame = myRoot.CFrame
            end
        end
    end
end

local function unanchorAreaEnemies(area)
    if not area then return end
    for _, obj in ipairs(area:GetDescendants()) do
        if obj:IsA("Model") then
            local hrp = obj:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.Anchored = false
            end
        end
    end
end

task.spawn(function()
    while true do
        local success, err = pcall(function()
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local myRoot = LocalPlayer.Character.HumanoidRootPart
                local RegionsLoaded = workspace:FindFirstChild("Gameplay") and workspace.Gameplay:FindFirstChild("RegionsLoaded")
                local Map = workspace:FindFirstChild("Gameplay") and workspace.Gameplay:FindFirstChild("Map")

                -- ===== FIRE =====
                local basicFire = Map and Map:FindFirstChild("ElementZones") and Map.ElementZones:FindFirstChild("Fire") and Map.ElementZones.Fire:FindFirstChild("Fire")
                if basicFire then
                    if getgenv().SaberFarmingStates.BringBasicFire then pullEnemiesToPlayer(basicFire, myRoot) else unanchorAreaEnemies(basicFire) end
                end
                local advFire = RegionsLoaded and RegionsLoaded:FindFirstChild("AdvancedFireArea") and RegionsLoaded.AdvancedFireArea:FindFirstChild("Important") and RegionsLoaded.AdvancedFireArea.Important:FindFirstChild("Fire")
                if advFire then
                    if getgenv().SaberFarmingStates.BringAdvancedFire then pullEnemiesToPlayer(advFire, myRoot) else unanchorAreaEnemies(advFire) end
                end
                local masterFire = RegionsLoaded and RegionsLoaded:FindFirstChild("MasterFireArea") and RegionsLoaded.MasterFireArea:FindFirstChild("Important") and RegionsLoaded.MasterFireArea.Important:FindFirstChild("Fire")
                if masterFire then
                    if getgenv().SaberFarmingStates.BringMasterFire then pullEnemiesToPlayer(masterFire, myRoot) else unanchorAreaEnemies(masterFire) end
                end
                local gmFire = RegionsLoaded and RegionsLoaded:FindFirstChild("GrandmasterFireArea") and RegionsLoaded.GrandmasterFireArea:FindFirstChild("Important") and RegionsLoaded.GrandmasterFireArea.Important:FindFirstChild("Fire")
                if gmFire then
                    if getgenv().SaberFarmingStates.BringGrandmasterFire then pullEnemiesToPlayer(gmFire, myRoot) else unanchorAreaEnemies(gmFire) end
                end

                -- ===== WATER =====
                local basicWater = Map and Map:FindFirstChild("ElementZones") and Map.ElementZones:FindFirstChild("Water") and Map.ElementZones.Water:FindFirstChild("Water")
                if basicWater then
                    if getgenv().SaberFarmingStates.BringBasicWater then pullEnemiesToPlayer(basicWater, myRoot) else unanchorAreaEnemies(basicWater) end
                end
                local advWater = RegionsLoaded and RegionsLoaded:FindFirstChild("AdvancedWaterArea") and RegionsLoaded.AdvancedWaterArea:FindFirstChild("Important") and RegionsLoaded.AdvancedWaterArea.Important:FindFirstChild("Water")
                if advWater then
                    if getgenv().SaberFarmingStates.BringAdvancedWater then pullEnemiesToPlayer(advWater, myRoot) else unanchorAreaEnemies(advWater) end
                end
                local masterWater = RegionsLoaded and RegionsLoaded:FindFirstChild("MasterWaterArea") and RegionsLoaded.MasterWaterArea:FindFirstChild("Important") and RegionsLoaded.MasterWaterArea.Important:FindFirstChild("Water")
                if masterWater then
                    if getgenv().SaberFarmingStates.BringMasterWater then pullEnemiesToPlayer(masterWater, myRoot) else unanchorAreaEnemies(masterWater) end
                end
                local gmWater = RegionsLoaded and RegionsLoaded:FindFirstChild("GrandmasterWaterArea") and RegionsLoaded.GrandmasterWaterArea:FindFirstChild("Important") and RegionsLoaded.GrandmasterWaterArea.Important:FindFirstChild("Water")
                if gmWater then
                    if getgenv().SaberFarmingStates.BringGrandmasterWater then pullEnemiesToPlayer(gmWater, myRoot) else unanchorAreaEnemies(gmWater) end
                end

                -- ===== EARTH =====
                local basicEarth = Map and Map:FindFirstChild("ElementZones") and Map.ElementZones:FindFirstChild("Earth") and Map.ElementZones.Earth:FindFirstChild("Earth")
                if basicEarth then
                    if getgenv().SaberFarmingStates.BringBasicEarth then pullEnemiesToPlayer(basicEarth, myRoot) else unanchorAreaEnemies(basicEarth) end
                end
                -- Note: Earth's advanced+ are inside "Model" subfolder?
                local advEarth = RegionsLoaded and RegionsLoaded:FindFirstChild("AdvancedEarthArea") and RegionsLoaded.AdvancedEarthArea:FindFirstChild("Important") and RegionsLoaded.AdvancedEarthArea.Important:FindFirstChild("Earth")
                if advEarth then
                    if getgenv().SaberFarmingStates.BringAdvancedEarth then pullEnemiesToPlayer(advEarth, myRoot) else unanchorAreaEnemies(advEarth) end
                end
                local masterEarth = RegionsLoaded and RegionsLoaded:FindFirstChild("MasterEarthArea") and RegionsLoaded.MasterEarthArea:FindFirstChild("Important") and RegionsLoaded.MasterEarthArea.Important:FindFirstChild("Earth")
                if masterEarth then
                    if getgenv().SaberFarmingStates.BringMasterEarth then pullEnemiesToPlayer(masterEarth, myRoot) else unanchorAreaEnemies(masterEarth) end
                end
                local gmEarth = RegionsLoaded and RegionsLoaded:FindFirstChild("GrandmasterEarthArea") and RegionsLoaded.GrandmasterEarthArea:FindFirstChild("Important") and RegionsLoaded.GrandmasterEarthArea.Important:FindFirstChild("Earth")
                if gmEarth then
                    if getgenv().SaberFarmingStates.BringGrandmasterEarth then pullEnemiesToPlayer(gmEarth, myRoot) else unanchorAreaEnemies(gmEarth) end
                end

                -- ===== PLASMA =====
                local basicPlasma = Map and Map:FindFirstChild("ElementZones") and Map.ElementZones:FindFirstChild("Plasma") and Map.ElementZones.Plasma:FindFirstChild("Plasma")
                if basicPlasma then
                    if getgenv().SaberFarmingStates.BringBasicPlasma then pullEnemiesToPlayer(basicPlasma, myRoot) else unanchorAreaEnemies(basicPlasma) end
                end
                local advPlasma = RegionsLoaded and RegionsLoaded:FindFirstChild("AdvancedPlasmaArea") and RegionsLoaded.AdvancedPlasmaArea:FindFirstChild("Important") and RegionsLoaded.AdvancedPlasmaArea.Important:FindFirstChild("Plasma")
                if advPlasma then
                    if getgenv().SaberFarmingStates.BringAdvancedPlasma then pullEnemiesToPlayer(advPlasma, myRoot) else unanchorAreaEnemies(advPlasma) end
                end
                local masterPlasma = RegionsLoaded and RegionsLoaded:FindFirstChild("MasterPlasmaArea") and RegionsLoaded.MasterPlasmaArea:FindFirstChild("Important") and RegionsLoaded.MasterPlasmaArea.Important:FindFirstChild("Plasma")
                if masterPlasma then
                    if getgenv().SaberFarmingStates.BringMasterPlasma then pullEnemiesToPlayer(masterPlasma, myRoot) else unanchorAreaEnemies(masterPlasma) end
                end
                local gmPlasma = RegionsLoaded and RegionsLoaded:FindFirstChild("GrandmasterPlasmaArea") and RegionsLoaded.GrandmasterPlasmaArea:FindFirstChild("Important") and RegionsLoaded.GrandmasterPlasmaArea.Important:FindFirstChild("Plasma")
                if gmPlasma then
                    if getgenv().SaberFarmingStates.BringGrandmasterPlasma then pullEnemiesToPlayer(gmPlasma, myRoot) else unanchorAreaEnemies(gmPlasma) end
                end
            end
        end)
        if not success then warn("[FarmLoop] Error:", err) end
        task.wait(0.08)
    end
end)

-- =============================================================================
-- 6. V2 TELEPORT-LOOP ENGINE (with guaranteed teleport)
-- =============================================================================

-- Helper: get element health (0 = dead)
local function getElementHealth(area)
    if not area then return 0 end
    for _, obj in ipairs(area:GetDescendants()) do
        if obj:IsA("Model") then
            local hrp = obj:FindFirstChild("HumanoidRootPart")
            if hrp then
                local ev = hrp:FindFirstChild("EnemyHealth")
                if ev and ev.Value then return ev.Value end
                local hum = obj:FindFirstChildOfClass("Humanoid")
                if hum then return hum.Health end
            end
        end
    end
    return 0
end

local function waitForAreaClear(area)
    if not area then return true end
    local timeout = 120
    local start = tick()
    while true do
        if getElementHealth(area) <= 0 then return true end
        if tick() - start > timeout then
            print("[V2] Timeout waiting for area to clear, moving on.")
            return true
        end
        task.wait(0.5)
    end
end

local function teleportTo(cf)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        hrp.CFrame = cf
        hrp.AssemblyLinearVelocity = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
        print("[V2] ✅ Teleported to", cf.Position)
    else
        warn("[V2] ❌ Character not ready for teleport")
    end
end

local function getPath(path)
    local curr = workspace
    for part in path:gmatch("[^%.]+") do
        curr = curr:FindFirstChild(part)
        if not curr then return nil end
    end
    return curr
end

-- Hardcoded fallback CFrames (from DEVIL script, verified working)
local FALLBACK_CFRAMES = {
    Fire = {
        Basic = { portal = CFrame.new(549.651, 189.897, 465.052), inner = CFrame.new(528.579, 189.898, 512.780) },
        Advanced = { portal = CFrame.new(191.011, -158.906, -1180.688), inner = CFrame.new(196.533, -128.283, -1181.272) },
        Master = { portal = CFrame.new(191.011, -158.906, -1180.688), inner = CFrame.new(196.533, -128.283, -1181.272) },
        Grandmaster = { portal = CFrame.new(191.011, -158.906, -1180.688), inner = CFrame.new(196.533, -128.283, -1181.272) }
    },
    Water = {
        Basic = { portal = CFrame.new(111.737, 287.194, -557.796), inner = CFrame.new(105.233, 281.259, -602.216) },
        Advanced = { portal = CFrame.new(822.279, -188.735, -1169.293), inner = CFrame.new(856.712, -170.465, -1206.762) },
        Master = { portal = CFrame.new(822.279, -188.735, -1169.293), inner = CFrame.new(856.712, -170.465, -1206.762) },
        Grandmaster = { portal = CFrame.new(822.279, -188.735, -1169.293), inner = CFrame.new(856.712, -170.465, -1206.762) }
    },
    Earth = {
        Basic = { portal = CFrame.new(790.708, 215.452, -484.461), inner = CFrame.new(779.851, 210.007, -533.011) },
        Advanced = { portal = CFrame.new(1313.346, -195.351, -1213.256), inner = CFrame.new(1302.567, -199.057, -1255.999) },
        Master = { portal = CFrame.new(1313.346, -195.351, -1213.256), inner = CFrame.new(1302.567, -199.057, -1255.999) },
        Grandmaster = { portal = CFrame.new(1313.346, -195.351, -1213.256), inner = CFrame.new(1302.567, -199.057, -1255.999) }
    },
    Plasma = {
        Basic = { portal = CFrame.new(8945.471, 6267.662, 1769.446), inner = CFrame.new(8984.981, 6261.662, 1760.883) },
        Advanced = { portal = CFrame.new(9571.298, 6363.610, 1608.676), inner = CFrame.new(9526.374, 6356.919, 1615.606) },
        Master = { portal = CFrame.new(9571.298, 6363.610, 1608.676), inner = CFrame.new(9526.374, 6356.919, 1615.606) },
        Grandmaster = { portal = CFrame.new(9571.298, 6363.610, 1608.676), inner = CFrame.new(9526.374, 6356.919, 1615.606) }
    }
}

-- Zone configurations (try dynamic first, fallback to hardcoded)
local function getZoneTeleports(element, tier)
    local configs = {
        Fire = {
            Basic = {
                portal = "Gameplay.Map.ElementZones.Fire.FireTeleport.FireTeleportPart1",
                inner = "Gameplay.Map.ElementZones.Fire.InnerCircle",
                area = workspace.Gameplay.Map.ElementZones.Fire.Fire
            },
            Advanced = {
                portal = "Gameplay.RegionsLoaded.AdvancedFireArea.Important.Teleport.FireTeleportPart2",
                inner = "Gameplay.RegionsLoaded.AdvancedFireArea.Important.Fire.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.AdvancedFireArea.Important.Fire
            },
            Master = {
                portal = "Gameplay.RegionsLoaded.MasterFireArea.Important.Teleport.FireTeleportPart2",
                inner = "Gameplay.RegionsLoaded.MasterFireArea.Important.Fire.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.MasterFireArea.Important.Fire
            },
            Grandmaster = {
                portal = "Gameplay.RegionsLoaded.GrandmasterFireArea.Important.Teleport.FireTeleportPart2",
                inner = "Gameplay.RegionsLoaded.GrandmasterFireArea.Important.Fire.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.GrandmasterFireArea.Important.Fire
            }
        },
        Water = {
            Basic = {
                portal = "Gameplay.Map.ElementZones.Water.WaterTeleport.WaterTeleportPart1",
                inner = "Gameplay.Map.ElementZones.Water.InnerCircle",
                area = workspace.Gameplay.Map.ElementZones.Water.Water
            },
            Advanced = {
                portal = "Gameplay.RegionsLoaded.AdvancedWaterArea.Important.Teleport.WaterTeleportPart2",
                inner = "Gameplay.RegionsLoaded.AdvancedWaterArea.Important.Water.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.AdvancedWaterArea.Important.Water
            },
            Master = {
                portal = "Gameplay.RegionsLoaded.MasterWaterArea.Important.Teleport.WaterTeleportPart2",
                inner = "Gameplay.RegionsLoaded.MasterWaterArea.Important.Water.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.MasterWaterArea.Important.Water
            },
            Grandmaster = {
                portal = "Gameplay.RegionsLoaded.GrandmasterWaterArea.Important.Teleport.WaterTeleportPart2",
                inner = "Gameplay.RegionsLoaded.GrandmasterWaterArea.Important.Water.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.GrandmasterWaterArea.Important.Water
            }
        },
        Earth = {
            Basic = {
                portal = "Gameplay.Map.ElementZones.Earth.Model.EarthTeleport.EarthTeleportPart1",
                inner = "Gameplay.Map.ElementZones.Earth.Model.Earth.InnerCircle",
                area = workspace.Gameplay.Map.ElementZones.Earth.Model.Earth
            },
            Advanced = {
                portal = "Gameplay.RegionsLoaded.AdvancedEarthArea.Important.Teleport.EarthTeleportPart2",
                inner = "Gameplay.RegionsLoaded.AdvancedEarthArea.Important.Earth.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.AdvancedEarthArea.Important.Earth
            },
            Master = {
                portal = "Gameplay.RegionsLoaded.MasterEarthArea.Important.Teleport.EarthTeleportPart2",
                inner = "Gameplay.RegionsLoaded.MasterEarthArea.Important.Earth.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.MasterEarthArea.Important.Earth
            },
            Grandmaster = {
                portal = "Gameplay.RegionsLoaded.GrandmasterEarthArea.Important.Teleport.EarthTeleportPart2",
                inner = "Gameplay.RegionsLoaded.GrandmasterEarthArea.Important.Earth.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.GrandmasterEarthArea.Important.Earth
            }
        },
        Plasma = {
            Basic = {
                portal = "Gameplay.Map.ElementZones.Plasma.PlasmaTeleport.PlasmaTeleportPart1",
                inner = "Gameplay.Map.ElementZones.Plasma.InnerCircle",
                area = workspace.Gameplay.Map.ElementZones.Plasma.Plasma
            },
            Advanced = {
                portal = "Gameplay.RegionsLoaded.AdvancedPlasmaArea.Important.Teleport.PlasmaTeleportPart2",
                inner = "Gameplay.RegionsLoaded.AdvancedPlasmaArea.Important.Plasma.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.AdvancedPlasmaArea.Important.Plasma
            },
            Master = {
                portal = "Gameplay.RegionsLoaded.MasterPlasmaArea.Important.Teleport.PlasmaTeleportPart2",
                inner = "Gameplay.RegionsLoaded.MasterPlasmaArea.Important.Plasma.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.MasterPlasmaArea.Important.Plasma
            },
            Grandmaster = {
                portal = "Gameplay.RegionsLoaded.GrandmasterPlasmaArea.Important.Teleport.PlasmaTeleportPart2",
                inner = "Gameplay.RegionsLoaded.GrandmasterPlasmaArea.Important.Plasma.InnerCircle",
                area = workspace.Gameplay.RegionsLoaded.GrandmasterPlasmaArea.Important.Plasma
            }
        }
    }
    return configs[element] and configs[element][tier]
end

-- Get teleport CFrame (dynamic + fallback)
local function getTeleportCFrame(element, tier, pathType)
    local config = getZoneTeleports(element, tier)
    local path = config and config[pathType]
    local dynamicPart = path and getPath(path)
    if dynamicPart then
        print("[V2] ✅ Dynamic " .. pathType .. " found for " .. tier .. " " .. element)
        return dynamicPart.CFrame
    else
        local fallback = FALLBACK_CFRAMES[element] and FALLBACK_CFRAMES[element][tier] and FALLBACK_CFRAMES[element][tier][pathType]
        if fallback then
            print("[V2] 🔄 Using fallback " .. pathType .. " for " .. tier .. " " .. element)
            return fallback
        else
            -- Ultimate fallback: try to get any BasePart from the area
            local area = config and config.area
            if area then
                if area:IsA("BasePart") then
                    print("[V2] 🔄 Using area as " .. pathType .. " for " .. tier .. " " .. element)
                    return area.CFrame
                elseif area:IsA("Model") and area.PrimaryPart then
                    print("[V2] 🔄 Using primary part as " .. pathType .. " for " .. tier .. " " .. element)
                    return area.PrimaryPart.CFrame
                end
            end
            warn("[V2] ❌ No " .. pathType .. " found for " .. tier .. " " .. element)
            return nil
        end
    end
end

-- Main V2 farm loop
task.spawn(function()
    local elementOrder = { "Fire", "Water", "Earth", "Plasma" }
    while true do
        if getgenv().SaberFarmingStates.V2.FarmActive then
            pcall(function()
                print("[V2] 🚀 Farm loop active!")
                for _, element in ipairs(elementOrder) do
                    local tiers = { "Basic", "Advanced", "Master", "Grandmaster" }
                    local enabledStages = {}
                    for _, tier in ipairs(tiers) do
                        if getgenv().SaberFarmingStates.V2[element][tier] then
                            local config = getZoneTeleports(element, tier)
                            if config and config.area then
                                table.insert(enabledStages, { tier = tier, config = config })
                                print("[V2] ✅ Enabled: " .. tier .. " " .. element)
                            else
                                print("[V2] ⚠️ No config/area for " .. tier .. " " .. element)
                            end
                        end
                    end

                    if #enabledStages == 0 then
                        goto continue_element
                    end

                    for _, stage in ipairs(enabledStages) do
                        if not getgenv().SaberFarmingStates.V2.FarmActive then break end

                        local area = stage.config.area
                        print("[V2] 🔥 Processing " .. stage.tier .. " " .. element)

                        -- Get portal CFrame
                        local portalCF = getTeleportCFrame(element, stage.tier, "portal")
                        if portalCF then
                            teleportTo(portalCF)
                            task.wait(0.5)
                        else
                            warn("[V2] ❌ No portal for " .. stage.tier .. " " .. element .. ", skipping")
                            goto continue_stage
                        end

                        -- Get inner circle CFrame
                        local innerCF = getTeleportCFrame(element, stage.tier, "inner")
                        if innerCF then
                            local offset = CFrame.new(15, 40, -37)
                            local targetCF = CFrame.new(
                                innerCF.Position + innerCF:VectorToWorldSpace(offset.Position),
                                innerCF.Position
                            )
                            teleportTo(targetCF)
                            task.wait(0.3)
                        else
                            print("[V2] ⚠️ No inner for " .. stage.tier .. " " .. element .. ", staying at portal.")
                        end

                        -- Wait for area to clear
                        print("[V2] ⏳ Farming " .. stage.tier .. " " .. element .. "...")
                        waitForAreaClear(area)
                        print("[V2] ✅ " .. stage.tier .. " " .. element .. " cleared!")

                        ::continue_stage::
                    end
                    ::continue_element::
                end
            end)
        end
        task.wait(0.2)
    end
end)
